services:
  - type: web
    name: hotel-booking-frontend
    env: static
    plan: free
    buildCommand: npm install && npm run build:prod
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://room-booking-backend-6wga.onrender.com
