{
  "name": "hotel-booking-backend",
  "version": "1.0.0",
  "description": "Hotel booking system backend",
  "main": "index.js",
  "bin": {
    "hotel-booking-backend": "./build.js"
  },
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "import-hotels": "node import_hotels.js",
    "build": "node scripts/build.js",
    "build:prod": "NODE_ENV=production node index.js",
    "prebuild": "echo 'Preparing build...'",
    "postbuild": "echo 'Build completed successfully'",
    "test": "echo 'No tests specified'",
    "lint": "echo 'No linter configured'",
    "clean": "echo 'Cleaning node_modules...' && rm -rf node_modules package-lock.json",
    "install:clean": "npm run clean && npm install",
    "health": "curl -f http://localhost:5000/health || exit 1",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "express-validator": "^7.2.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
